import { Header } from './Header/Header';
// import { useTheme } from './Theme/use-Theme';

export const App = () => {
  // const [theme, setTheme] = useTheme();
  return (
    <div>
      <Header />
    </div>
  );
};
